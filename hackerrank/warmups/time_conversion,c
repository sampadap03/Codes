#include<stdio.h>
#include<stdlib.h>
int main()
{
    char hr[2],min[20],str[20];
    int num;
    scanf("%s:%s",hr,min);
   
    strncpy(str,min,6);
    if(min[6]=='A') printf("%s%s",hr,min);
    num=atoi(hr);
    num+=12;
    printf("%d%s",num,str);
    return 0;
}
