/*Nitish had a lemons, b apples and c pears. He decided to make a dessert.The recipe says: the fruits should be in the ratio 1: 2: 4. It means that for each lemon in the dessert should be exactly 2 apples and exactly 4 pears. You can't crumble up, break up or cut fruits into pieces.Fruits — lemons, apples and pears — should be put in the dessert as whole fruits.

Your task is to determine the maximum total number of lemons, apples and pears from which Nitish can make the dessert. It is possible that Nitish can't use any fruits, in this case print 0.

Input Format

The first line contains the positive integer a the number of lemons available.

The second line contains the positive integer b the number of apples available.

The third line contains the positive integer c the number of pears available.

Constraints

1 ≤ a,b,c ≤ 1000

Output Format

Print the maximum total number of lemons, apples and pears from which Nitish can make the dessert*/

#include<stdio.h>
int main()
{
int l,a,p;
scanf("%d",&l);
scanf("%d",&a);
scanf("%d",&p);

}

if(l!=0&&a<2&&)




















